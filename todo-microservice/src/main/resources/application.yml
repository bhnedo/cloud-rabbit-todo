spring:
  application:
    name: todo-microservice

  data:
    mongodb:
      port: 27017
      host: localhost
      database: rabbit-todo

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/